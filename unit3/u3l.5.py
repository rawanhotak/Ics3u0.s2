Enter a loop
   go to the next array member (N)
   Compare N to highest
   If highest < N 
      highest = N
After the loop exits, you should have the highest array member in the variable "highest"

def add(a: float, b: float) -> float:
   sum = a + b
   return sum

print(add(7, 2))
